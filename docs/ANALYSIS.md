# 📊 การวิเคราะห์เปรียบเทียบ: VM vs Docker Deployment
## ENGSE207 - Week 6 N-Tier Architecture

**ชื่อ-นามสกุล:** [นายณับวิโรจน์ สุทธิธารมงคล]  
**รหัสนักศึกษา:** [67543210026-0]  
**วันที่:** [13 ก.พ. 2026]

---

## 1. ตารางเปรียบเทียบ Setup Process

| ขั้นตอน | Version 1 (VM) | Version 2 (Docker) |
|---------|----------------|-------------------|
| ติดตั้ง PostgreSQL | [ใช้ apt install และตั้งค่า manual] | [ใช้ docker-compose pull image] |
| ติดตั้ง Node.js | [ติดตั้งผ่าน apt / nvm] | [อยู่ใน image แล้ว] |
| ติดตั้ง Nginx | [ติดตั้ง + config manual] | [container พร้อมใช้] |
| Configure Database | [สร้าง DB + user + permission] | [ENV + auto init script] |
| Configure SSL | [generate cert + config nginx] | [mount cert เข้า container] |
| Start Services | [systemctl start ทีละ service] | [docker compose up] |
| **เวลาทั้งหมด** | [~25–40 นาที] | [~3–5 นาที] |

---

## 2. ตารางเปรียบเทียบ Resource Usage

| Resource | Version 1 (VM) | Version 2 (Docker) |
|----------|----------------|-------------------|
| Memory Usage | [ใช้ `~700MB–1.2GB`] | [ใช้ `~150–350MB`] |
| Disk Usage | [ใช้ `~8–15GB`] | [ใช้ `~1–2GB`] |
| CPU Usage | [สูงกว่า (มี OS เพิ่ม)] | [ต่ำกว่า] |
| Startup Time | [60–120s] | [3–10s] |

---

## 3. ข้อดีของ Docker Deployment (เขียน 5 ข้อ)

1. **[Deploy เร็วมาก]:** [ใช้คำสั่งเดียว docker compose up ก็รันทั้ง stack ได้ทันที]

2. **[Environment เหมือนกันทุกเครื่อง]:** [ไม่เกิดปัญหา “เครื่องฉันรันได้แต่เครื่องเพื่อนรันไม่ได้”]

3. **[แยก service ชัดเจน]:** [DB / API / Nginx อยู่คนละ container ไม่ชนกัน]

4. **[Scale ง่าย]:** [เพิ่ม instance API ได้ทันทีโดยเพิ่ม replica]

5. **[Rollback ง่าย]:** [แค่เปลี่ยน image tag แล้ว redeploy]

---

## 4. ข้อเสียของ Docker Deployment (เขียน 3 ข้อ)

1. **[Debug ยากกว่า]:** [ต้องเข้า container เพื่อดู log หรือ test]

2. **[Learning curve สูง]:** [ต้องเข้าใจ networking / volume / image layer]

3. **[Resource isolation]:** [ถ้ากำหนด limit ผิด container อาจ crash]

---

## 5. เมื่อไหร่ควรใช้ VM vs Docker?

### ควรใช้ VM เมื่อ:
- ต้องจำลองระบบทั้ง OS จริง

- ต้องรัน software ที่ต้องใช้ kernel feature

- ใช้ legacy system ที่ container ไม่รองรับ

### ควรใช้ Docker เมื่อ:
- Deploy web app / API

- ต้องการ CI/CD automation

- ต้อง scale service หลาย instance

---

## 6. สิ่งที่ได้เรียนรู้จาก Lab นี้

จาก Lab นี้ได้เรียนรู้ความแตกต่างระหว่างการ deploy แบบ VM และ Docker อย่างชัดเจน โดย Docker ช่วยลดเวลา setup และทำให้ environment มีความสม่ำเสมอมากขึ้น อีกทั้งยังเข้าใจแนวคิด containerization, health check, networking และ reverse proxy ด้วย Nginx ซึ่งเป็นพื้นฐานสำคัญของระบบ production จริง ทำให้เห็นว่า Docker เหมาะกับการพัฒนาและ deploy ระบบสมัยใหม่มากกว่า VM ในหลายกรณี

---

## 7. คำสั่ง Docker ที่ใช้บ่อย (Quick Reference)

```bash
docker compose up
docker compose down
docker compose build
docker ps
docker stats
```
